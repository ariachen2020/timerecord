# 🚀 Getting Started - 開始使用

## 第一次使用本專案？

歡迎！這個專案已經完全建置完成，你可以立即開始使用。

## 📁 專案結構

```
timerecord/
├── api/                          # 後端 API
│   ├── src/
│   │   └── index.js             # Express 伺服器（所有 API）
│   ├── package.json
│   └── .env.example
├── database/                     # 資料庫
│   ├── schema.sql               # 資料庫結構
│   └── migrate.js               # 遷移腳本
├── src/                          # 前端原始碼
│   ├── api/
│   │   └── client.js            # API 客戶端
│   ├── components/
│   │   └── Layout.jsx           # 布局組件
│   ├── context/
│   │   └── AuthContext.jsx     # 認證上下文
│   ├── pages/
│   │   ├── Login.jsx            # 登入頁面
│   │   ├── Dashboard.jsx        # 首頁總覽
│   │   ├── AddRecord.jsx        # 新增記錄
│   │   └── QueryEmployee.jsx   # 查詢員工
│   ├── utils/
│   │   └── timeFormat.js        # 工具函數
│   ├── App.jsx                  # 主應用
│   └── main.jsx                 # 入口點
├── public/                       # 靜態資源
├── index.html                    # HTML 模板
├── package.json                  # 前端依賴
├── vite.config.js               # Vite 配置
├── tailwind.config.js           # Tailwind 配置
├── zeabur.json                  # Zeabur 部署配置
├── README.md                     # 完整文件
├── QUICKSTART.md                # 快速開始
├── PROJECT_SUMMARY.md           # 專案總結
└── GETTING_STARTED.md           # 本文件
```

## ⚡ 快速啟動（3 步驟）

### 步驟 1: 安裝依賴

```bash
# 安裝前端依賴
npm install

# 安裝後端依賴
cd api
npm install
cd ..
```

### 步驟 2: 設定資料庫

```bash
# 建立 PostgreSQL 資料庫
createdb timerecord

# 執行資料庫結構
psql timerecord < database/schema.sql
```

### 步驟 3: 設定環境變數

#### 前端 (根目錄 `.env`)
```bash
echo "VITE_API_URL=http://localhost:3000" > .env
```

#### 後端 (`api/.env`)
```bash
cat > api/.env << 'ENVEOF'
DATABASE_URL=postgresql://localhost:5432/timerecord
SESSION_SECRET=dev-secret-change-in-production
PORT=3000
FRONTEND_URL=http://localhost:5173
DEPARTMENTS={"HR":{"name":"人資部","username":"hr","password":"hr123"},"IT":{"name":"資訊部","username":"it","password":"it123"}}
ENVEOF
```

## 🎮 啟動應用

開啟兩個終端視窗：

### 終端 1 - 後端 API
```bash
cd api
npm run dev
```

你會看到:
```
╔════════════════════════════════════════╗
║   補休登錄系統 - API 伺服器           ║
╚════════════════════════════════════════╝

🚀 伺服器啟動成功！
📡 端口: 3000
```

### 終端 2 - 前端
```bash
npm run dev
```

你會看到:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## 🌐 訪問應用

打開瀏覽器訪問: **http://localhost:5173**

### 預設測試帳號
- **人資部**: 帳號 `hr` / 密碼 `hr123`
- **資訊部**: 帳號 `it` / 密碼 `it123`

## 🧪 測試功能

### 1️⃣ 測試登入
```
1. 訪問 http://localhost:5173
2. 輸入帳號: hr
3. 輸入密碼: hr123
4. 點擊登入
✅ 應該成功登入並看到首頁
```

### 2️⃣ 測試新增補休
```
1. 點擊「新增記錄」
2. 員工號碼: A001
3. 操作類型: 增加
4. 小時: 8
5. 分鐘: 0
6. 日期: (選擇今天)
7. 原因: 加班
8. 點擊「確認增加」
✅ 應該顯示成功訊息
```

### 3️⃣ 測試查詢
```
1. 點擊「查詢員工」
2. 輸入: A001
3. 點擊查詢
✅ 應該看到剛才新增的 8 小時補休
```

### 4️⃣ 測試使用補休
```
1. 點擊「新增記錄」
2. 員工號碼: A001
3. 操作類型: 減少
4. 小時: 4
5. 點擊「確認減少」
✅ 餘額應該剩下 4 小時
```

### 5️⃣ 測試 FIFO 邏輯
```
# 新增多筆不同日期的補休
A001 - 2024-01-01 增加 8 小時
A001 - 2024-06-01 增加 6 小時
A001 - 2024-11-01 增加 4 小時

# 使用 10 小時
A001 - 減少 10 小時

# 查詢結果應該:
✅ 1/1 的 8 小時全部扣除
✅ 6/1 的 6 小時扣 2 小時（剩 4 小時）
✅ 11/1 的 4 小時未動
✅ 總餘額: 8 小時
```

## 📚 下一步

### 深入了解
- 閱讀 [README.md](README.md) 了解完整功能
- 查看 [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) 了解技術細節
- 參考 [QUICKSTART.md](QUICKSTART.md) 快速參考指令

### 部署到生產環境
- 閱讀 README.md 的 Zeabur 部署章節
- 記得修改預設密碼
- 設定正確的環境變數

### 自訂功能
- 修改 `api/.env` 的 DEPARTMENTS 新增更多部門
- 調整 `src/pages/` 的頁面樣式
- 擴充 `api/src/index.js` 新增更多 API

## 🐛 遇到問題？

### 常見錯誤解決

#### 錯誤 1: 無法連接資料庫
```
❌ 資料庫連線失敗
```
**解決方法**:
1. 確認 PostgreSQL 已啟動: `pg_isready`
2. 檢查 DATABASE_URL 是否正確
3. 確認資料庫已建立: `psql -l | grep timerecord`

#### 錯誤 2: 前端無法連接後端
```
❌ Network Error
```
**解決方法**:
1. 確認後端在 port 3000 運行
2. 檢查 .env 的 VITE_API_URL
3. 查看瀏覽器 Console 錯誤訊息

#### 錯誤 3: Session 無法保持
```
❌ 登入後立即登出
```
**解決方法**:
1. 確認 session 表已建立
2. 檢查 SESSION_SECRET 是否設定
3. 確認 CORS 設定正確

### 需要更多幫助？
- 查看專案 Issues
- 提交新的 Issue 描述問題

## 🎉 恭喜！

你已經成功啟動補休登錄系統！開始探索所有功能吧！

---

**提示**: 建議先在本地完整測試所有功能，確認運作正常後再部署到生產環境。
