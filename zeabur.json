{
  "version": "2",
  "services": [
    {
      "name": "timerecord",
      "buildCommand": "npm install && npm run build && cd api && npm install",
      "startCommand": "cd api && npm start",
      "env": {
        "NODE_ENV": "production"
      }
    }
  ]
}
